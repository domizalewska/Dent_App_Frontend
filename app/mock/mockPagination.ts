import type { PaginationData } from '~/types/api/pagination.types'

export const mockPagination: PaginationData = {
  current_page: 1,
  from: 1,
  last_page: 1,
  per_page: 15,
  to: 15,
  total: 15,
}
